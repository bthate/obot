[Unit]
Description=pure python3 channel bot

[Service]
ExecStart=/usr/local/bin/obot -k -d /var/lib/obot --logdir /var/log/obot -l debug --owner root@shell -z -b

[Install]
WantedBy=multi-user.target
